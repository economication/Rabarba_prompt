[project]
name = "rabarba-prompt"
version = "0.1.0"
description = "Local-first LangGraph-based prompt optimizer for coding agents"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"
